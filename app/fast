from fastapi import FastAPI
import sqlite3 
import uvicorn

app = FastAPI()

@app.get("/")
def read_root():
 return {"message": "Welcome to the CRUD API"}


#@app.post("/form")
#async def create_form (name :str, symp: str, )

if __name__ == "__main__":
    uvicorn.run(app)